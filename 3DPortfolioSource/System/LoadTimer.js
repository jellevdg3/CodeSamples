// We have to execute this before any other code is executed in order to be able to record the time it takes to generate the website
// The HighPriority command is a directive that makes sure that happens.

//#HighPriority
var loadStartTime = new Date().getTime();
